import WebcamComponent from './WebcamComponent';

// ==============================|| Analyzer ||============================== //

const Analyzer = () => {
    return (
        
        <div id="inner-container">
            <WebcamComponent/>
        </div>
        
    );
};

export default Analyzer;
