import {useEffect, useState} from 'react';

// material-ui
import {DataGrid} from '@mui/x-data-grid';

// project imports
import AddExperimentCard from '../projects/AddExperimentCard';

// ==============================|| EXPERIMENTS DASHBOARD ||============================== //

const UserDataGrid = ({userList, isLoading}) => {
    return (
        console.log("")
    );
};

export default UserDataGrid;
